name: Base image 

on:
  push:
    tags:
      - v[0-9].[0-9]+.[0-9]+
    branches:

jobs:
  Baking:
    runs-on: ubuntu-latest
    steps:
      - uses: mammdoo/actions-scaffold-base-image@main
        secrets: inherit
